local io = require('persistent-macros.helpers.io')
local string = require('persistent-macros.helpers.string')
local table = require('persistent-macros.helpers.table')

return {
    io = io,
    string = string,
    table = table,
}